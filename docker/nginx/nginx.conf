# internal docker DNS resolver so upstreams via variables work correctly
# https://docs.docker.com/v17.09/engine/userguide/networking/configure-dns/

resolver 127.0.0.11;

server {
    listen 80;
    server_name beam.press;

    root /var/www/html/public;

    location / {
        try_files $uri /index.php$is_args$args;
    }

    location ~ ^/.+\.php(/|$) {
        set $upstream beam:9000;
        fastcgi_pass $upstream;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }
}

server {
    listen 80;
    server_name tracker.beam.press;

    location / {
        set $upstream beam_tracker:8081;
        proxy_pass http://$upstream;
    }
}

server {
    listen 80;
    server_name segments.beam.press;

    location / {
        set $upstream beam_segments:8082;
        proxy_pass http://$upstream;
    }
}

server {
    listen 80;
    server_name kibana.beam.press;

    location / {
        set $upstream kibana:5601;
        proxy_pass http://$upstream;
    }
}


# Required by websocket configuration (see mailhog server configuration)
map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}